export default function WatchListItem({ product }) {
	// WatchList item should render the product name, price, and image.
	// It should also have a button that when clicked, will remove the item from the watchList.
}
